Global setup:

 Set up git
  git config --global user.name "Kaz"
  git config --global user.email sy250f@gmail.com
      

Next steps:

  mkdir test
  cd test
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:sy250/test.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:sy250/test.git
  git push -u origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue

@sy250f added 2016.08.03
